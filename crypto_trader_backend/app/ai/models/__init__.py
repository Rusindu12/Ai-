"""Model implementations.

``tensorflow`` / ``torch`` are optional: when absent the NumPy implementations are
used, and when present :mod:`app.ai.export` converts the same weights to
TFLite / ONNX for on-device inference.
"""
