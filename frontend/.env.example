# The browser talks to the backend through these URLs.
# NEXT_PUBLIC_API_URL is used by next.config.mjs to rewrite /api/* -> backend.
# NEXT_PUBLIC_WS_URL is used by the Socket.io client.
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=http://localhost:4000
