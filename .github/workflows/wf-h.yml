name: DBG apk needs-missing
on:
  push:
    branches: ["**"]
jobs:
  apk:
    needs: quality
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
