<?xml version="1.0" encoding="utf-8"?>
<!-- Production traffic must be TLS. Cleartext is only allowed to loopback/LAN
     addresses so you can develop against a local backend without headaches.
     For SSL pinning add your leaf cert to res/raw/ and uncomment pins below. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.100</domain>
    </domain-config>

    <!--
    <domain-config>
        <domain includeSubdomains="true">api.your-domain.com</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">BASE64_SPKI_SHA256_OF_YOUR_CERT=</pin>
            <pin digest="SHA-256">BASE64_SPKI_SHA256_OF_BACKUP_CERT=</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>
