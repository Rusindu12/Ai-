<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug/profile builds only: allow HTTP to a local dev backend. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
